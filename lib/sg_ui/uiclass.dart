import 'package:flutter/material.dart';

class Uiclass extends StatelessWidget {
  const Uiclass({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
